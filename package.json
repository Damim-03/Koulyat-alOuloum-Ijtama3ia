{
  "name": "koulyat-alouloum-ijtama3ia",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../Backend && npm install && npx prisma generate && npx prisma migrate deploy && npm run build",
    "start": "cd Backend && node dist/app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "tsx": "^4.23.0"
  }
}
