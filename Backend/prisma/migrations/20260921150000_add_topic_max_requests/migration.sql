-- سقفُ المحاولات على الموضوع الواحد، بما فيها المرفوضة.
--
-- والفهرسُ الفريد على `GroupRequest.activeTopicId` يمنع أصلاً وجودَ أكثر من
-- طلبٍ حيٍّ واحد على الموضوع. فهذا العمود لا يحدّ المتزامنَ — إنّما يحدّ
-- المتراكم: فريقٌ رُفض طلبُه يستطيع اليوم إعادته بلا نهاية، وهذا يوقفه.
--
-- و`NULL` تعني بلا سقف، فالمواضيع القائمة تبقى على حالها ولا تسقط الهجرة
-- على جدولٍ عامر.
--
-- واسمُ الجدول بحالة أحرفه الصحيحة: حسّاسٌ لحالته على لينكس.

ALTER TABLE `GraduationTopic` ADD COLUMN `maxRequests` INTEGER NULL;
