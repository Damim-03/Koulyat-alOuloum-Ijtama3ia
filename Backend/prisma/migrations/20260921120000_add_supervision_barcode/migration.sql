-- الرمزُ الشريطيّ أسفل ورقة الموافقة: ثلاثة عشر رقماً بصيغة EAN-13.
--
-- يُمسح بقارئٍ عاديّ أو يُكتب باليد في صفحة التحقّق، فيصلح لمن لا هاتف
-- ذكيّ معه ولمن يعمل على حاسوب الأرشيف.
--
-- و`NULL` مسموح: الجدول قد يحمل أوراقاً صدرت قبل هذا العمود، ولو فُرض
-- `NOT NULL` لسقطت الهجرة عليها. والخدمة تمنح كلّ ورقةٍ رمزَها عند أوّل
-- قراءة، فلا تبقى ورقةٌ بلا رمز.
--
-- واسمُ الجدول بحالة أحرفه الصحيحة: حسّاسٌ لحالته على لينكس.

ALTER TABLE `SupervisionDocument` ADD COLUMN `barcode` VARCHAR(191) NULL;

CREATE UNIQUE INDEX `SupervisionDocument_barcode_key` ON `SupervisionDocument`(`barcode`);
