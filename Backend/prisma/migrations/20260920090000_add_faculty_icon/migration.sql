-- شعارُ الكلّية، مستقلًّا عن غلافها.
--
-- `coverUrl` خلفيةٌ عريضة تُقرأ خلف البطاقة؛ وهذا علامةٌ مربّعة تحلّ محلّ
-- أيقونة المبنى الافتراضية وتُقرأ في أربعين بكسلاً. ولذلك عمودان لا عمود:
-- صورةٌ واحدة لا تصلح للموضعين.
--
-- وأسماء الجداول بحالة أحرفها الصحيحة — انظر `add_cover_images`: الاسم
-- حسّاسٌ لحالته على لينكس، فهجرةٌ بأحرفٍ صغيرة تمرّ على ويندوز وتمنع لينكس.

-- AlterTable
ALTER TABLE `Faculty` ADD COLUMN `iconUrl` VARCHAR(191) NULL;
