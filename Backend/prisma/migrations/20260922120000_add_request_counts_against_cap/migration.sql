-- تمييزُ المحاولة الحقيقية من الإنهاء الإداريّ في حساب سقف الطلبات.
--
-- فسخُ المشروع يُعلّم الطلبَ المقبول `rejected` ليُفرِج عن الموضوع. وبلا هذا
-- العمود يُحسب الفسخُ محاولةً على السقف — فيُغلق الموضوع الذي فسخته الإدارة
-- لِيُتداول من جديد: يدٌ تفتح وأخرى تُقفل.
--
-- والافتراضُ `TRUE` فالصفوفُ القائمة تبقى محسوبةً كما كانت، وهو الصواب:
-- كلُّها طلباتٌ أرسلتها فرق.

ALTER TABLE `GroupRequest` ADD COLUMN `countsAgainstCap` BOOLEAN NOT NULL DEFAULT TRUE;
