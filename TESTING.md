# الاختبارات

## البدء السريع

```bash
# الخلفية
cd Backend && npm run test:run

# الواجهة
cd client && npm run test:run
```

أوّل مرّة فقط، لإنشاء قاعدة الاختبار وتطبيق الهجرات عليها:

```bash
cd Backend && npm run test:db
```

---

## الطبقات

| الطبقة | أين | ماذا تُثبت | العدد |
|---|---|---|---|
| **وحدة — خلفية** | `Backend/src/**/*.test.ts` | منطق صرف بلا قاعدة: الإسقاط، القرار، جدول القواعد، تصديق الملفّات، معالجة الأخطاء | ١٠١ |
| **تكامل** | `Backend/tests/integration/*.test.ts` | الخدمات على قاعدة حقيقية: الثوابت الـ١٠ | ٣٥ |
| **API** | `Backend/tests/api/*.test.ts` | HTTP حقيقي وسوكيت حقيقي: الصلاحيات، رموز الحالة، وصول رسائل الخادم، عزل الغرف، الرفع، اللوحات، الجرس | ٦٨١ |
| **وحدة — واجهة** | `client/src/**/*.test.ts` | قراءة حكم الخادم، ومكوّنات React | ٩ |
| **متصفّح (E2E)** | `client/e2e/*.spec.ts` | التطبيق كلّه يعمل: الدخول، الحُرّاس، وأنّ الشاشات تُعرَض | ٣٦ |

المجموع: **٨٦٢ اختباراً** — نحو ١٥٠ ثانية للخلفية، و٦٠ لوحدات الواجهة، و٥ دقائق للمتصفّح.

ومن الـ٥٤٧ في طبقة API، ٢٣٨ كنسةٌ آلية تمرّ على كل مسارٍ في التطبيق
(`route-guards.api.test.ts`) فتتحقّق أنه يردّ ٤٠١ بلا رمز و٤٠٣ بدورٍ خاطئ؛
والباقي اختبارات سلوكٍ مكتوبةٌ بيد. و`npm run endpoints` يقول أيّ المسارات
له وعدٌ موصوف وأيّها محروسٌ فقط — وهو اليوم **١٣٢/١٣٢ (١٠٠٪)**: لم يبقَ مسارٌ
في التطبيق بلا اختبارٍ يصف ما يَعِد به. والمقياس هذا يحرس نفسه: كل مسارٍ جديد
يُضاف يظهر في التقرير ناقصاً حتى يُكتب له اختبار.

وملفٌّ واحد فيها يخرج عن القاعدة: `realtime-socket.api.test.ts` يستورد
**`server.ts` نفسه** لا `app.ts` — لأن Socket.IO يُركَّب هناك، ولأن اختباراً
يبني خادماً مكافئاً يُثبت أن `installSocketSecurity` تعمل، لا أن أحداً
يستدعيها. ولذلك `PORT=0` في `.env.test` (وفي CI): «أعطني أي منفذٍ حرّ»، حتى
لا يأخذ الاختبار منفذ خادم التطوير من صاحبه.

الاختبار يعيش **بجوار ما يختبره** (`topic-status.ts` ← `topic-status.test.ts`)، إلا اختبارات API فهي في `tests/` لأنها لا تخصّ ملفّاً بعينه.

---

## الاستعمال اليومي

```bash
npm test              # وضع المراقبة — يُعيد التشغيل عند كل حفظ
npm run test:run      # مرّة واحدة (قبل الـ commit، وفي CI)
npm run test:cov      # تقرير التغطية → coverage/index.html
npm test -- topic     # الملفّات التي يحوي اسمها "topic" فقط
```

**وضع المراقبة هو الاستعمال الحقيقي.** اتركه يعمل في نافذة جانبية وأنت تكتب؛ تكسر شيئاً فيحمرّ في ثانية، لا بعد ساعة.

---

## قاعدة الاختبار

اختبارات API تكتب في قاعدة **منفصلة**: `kouliate_ouloum_test`، إعدادها في `Backend/.env.test` (خارج git).

وهذا ليس تنظيماً بل حماية. لذلك في `tests/load-env.ts` حارسٌ **يرفض التشغيل** إن لم ينتهِ اسم القاعدة بـ`_test`:

```
الاختبارات تُنشئ الصفوف وتحذفها، ورُفض التشغيل على قاعدة اسمها «kouliate_ouloum».
```

ملفّ `.env.test` مفقود أو مضبوط خطأً يعني أن الاختبارات ستكتب في قاعدة تطويرك بصمت. الحارس يجعل ذلك مستحيلاً.

كل صفّ تُنشئه الاختبارات يحمل الوسم `__TEST__`، و`afterAll` يحذفه ثم **يتحقّق أن العدد صفر**.

---

## كيف تكتب اختباراً

### القاعدة الأولى: الفشل قبل النجاح

> كل عطل يُكتب له اختبار **فاشل أوّلاً**، ثم يُصلَح حتى يخضرّ.

اختبار لم ترَه يفشل لا تعرف أنه يختبر شيئاً. هذه ليست نظرية: في هذا المشروع كتبتُ اختباراً لثغرة حقيقية فجاء **أخضر** — لأن الطلبة أُعيد استعمالهم بين السيناريوهات فرفض الخادم لسبب آخر تماماً. لو لم يُختبر منفرداً لسُلِّم حارسٌ لا وجود له.

### القاعدة الثانية: اختبر السلوك لا التنفيذ

```ts
// ✗ يكسره أي إعادة هيكلة
expect(setTopicDecision).toHaveBeenCalledWith(prisma, id, "approved");

// ✓ يبقى صالحاً مهما تغيّر الداخل
expect(await statusOf(id)).toBe("approved");
```

### القاعدة الثالثة: اسم الاختبار جملة تصف قاعدة

```ts
it("موضوع منشور أُرشف يعود open لا approved — الأرشفة لا تُلغي النشر", ...)
```

حين يحمرّ هذا السطر بعد سنة، تعرف ما انكسر بلا قراءة الشيفرة.

### مثال — وحدة (بلا قاعدة)

```ts
import { projectTopicStatus } from "./topic-status";

it("المجموعة تغلب النشر: full", () => {
  expect(
    projectTopicStatus({ decision: "approved", publishedAt: new Date(), hasGroup: true })
  ).toBe("full");
});
```

### مثال — API

```ts
it("يرفض حذف موضوع تشكّلت له مجموعة ⇒ 400 ورسالة تقول ماذا يُفعل بدلاً", async () => {
  const res = await request(app)
    .delete(`/api/admin/topics/${topic.id}`)
    .set("Authorization", `Bearer ${adminToken}`)
    .expect(400);

  expect(res.body.message).toContain("أرشفه");
});
```

---

## ملاحظات على البيئة

**`PATH` عندك ٩٥٣٧ حرفاً** (٢٣٩ مدخلاً، ٤٠ مكرَّراً)، وحدّ `cmd.exe` نحو ٨١٩١. فأي حزمة تحتاج بناءً أصلياً تفشل بـ`'node' is not recognized`. الحلّ الدائم: نظّف متغيّرات البيئة من لوحة التحكّم. والمؤقّت قبل `npm install`:

```powershell
$env:PATH = ($env:PATH -split ';' | Where-Object { $_ } | Select-Object -Unique) -join ';'
```

**Vitest يستعمل `pool: "threads"`** لا `forks`، لأن مسار المشروع فيه مسافة و`forks` يفشل عليه بمهلة صامتة.

**حين تنفجر المخرجات:** خطأ من Prisma يجعل Jest يطبع «إطار الشيفرة» حول موضعه — وموضعه داخل العميل المولَّد في `src/generated`، وهو ملفّ مصغَّر بأسطر طولها آلاف الأحرف. فخطأ واحد يملأ ثلاث شاشات. عندها:

```bash
npm run test:run -- --noStackTrace
```

يطبع الرسالة وحدها. لا تجعله الافتراضي: أثر الكومة نافع حين يُرمى خطأ غير متوقَّع من داخل خدمة وتريد معرفة أي سطر رماه.

---

## أدوات التشغيل (ليست اختبارات)

هاتان تقرآن **قاعدتك الحقيقية** وتُصلحانها — وهذا شغلهما، فلم تُنقلا إلى الإطار:

```bash
npm run audit:topics      # تدقيق ثوابت بياناتك الحالية (قراءة فقط)
npm run backfill:leaders  # إصلاح المجموعات بلا قائد (عرض فقط، و--apply ليكتب)
```

أمّا `check:topics` و`check:clean` فقد تقاعدا: صارا `tests/integration/topic-status.test.ts`.

---

## متى تُشغَّل الاختبارات

| الوقت | ماذا | لماذا |
|---|---|---|
| **وأنت تكتب** | `npm test` في نافذة جانبية | يحمرّ في ثانية، لا بعد ساعة |
| **قبل كل commit** | `npm run test:run` | لا تُسجّل شيئاً أحمر |
| **قبل الدمج / الرفع** | `test:run` في الطرفين + `npm run build` | آخر بوّابة قبل أن يراه غيرك |
| **بعد كل عطل تُبلَّغ به** | اكتب اختباراً **فاشلاً** يعيد إنتاجه، ثم أصلحه | العطل لا يعود مرّتين |
| **أسبوعياً** | `npm run test:cov` | اقرأ الخريطة، لا الرقم |

## التشغيل الآلي (CI)

`.github/workflows/test.yml` — يعمل على كل دفع وكل طلب دمج نحو `main`.

| الوظيفة | ماذا تفعل |
|---|---|
| **الخلفية** | MariaDB 10.4 في حاوية · هجرات · فحص أنواع مرّتين · ١٧٤ اختباراً بالتغطية · بناء |
| **الواجهة** | فحص أنواع · اختبارات · بناء |
| **كل شيء أخضر** | تفشل إن فشلت إحداهما — تُستعمل شرطاً للدمج |

وتقرير التغطية يُرفع كمُخرَج قابل للتنزيل من صفحة كل تشغيل.

**لا أسرار حقيقية في الملفّ.** الأسرار فيه للتشغيل الآلي وحده، على قاعدة تُنشأ وتُهدَم مع كل تشغيل. أسرار الإنتاج تعيش في إعدادات المستودع لا في ملفّ مسجَّل.

و`.env.test` **لا يُنشأ** في CI: هو ملفّ محلّي خارج git، و`tests/load-env.ts` يتجاوزه بصمت حين لا يجده فتُقرأ متغيّرات البيئة. الحارس الذي يرفض قاعدة لا تنتهي بـ`_test` يبقى عاملاً في الحالتين.

### جعل النجاح شرطاً للدمج

بعد أوّل تشغيل ناجح، من إعدادات المستودع:

`Settings → Branches → Add rule` على `main` ⇒ فعّل **Require status checks to pass** واختر **كل شيء أخضر**.

عندها لا يُدمج شيء أحمر، ولو نسي أحدهم تشغيل الاختبارات.

---

---

## اختبارات المتصفّح (Playwright)

```bash
cd client
npm run e2e          # تشغيل كامل
npm run e2e:ui       # واجهة تفاعلية للاستكشاف
npm run e2e:report   # آخر تقرير
```

الإعداد يفعل كل شيء بنفسه: يبذر قاعدة الاختبار، ويبني الواجهة، ويُشغّل
الخادمين، ثم ينظّف بعده. لا خطوة يدوية واحدة.

**ما تُثبته هذه الطبقة وحدها.** الخلفية موصوفةٌ بالكامل، لكن بين ردّ الخادم
وبين ما يقع على الشاشة طبقةٌ لا يمسّها اختبارٌ آخر: التوجيه، وحفظ الرمز،
وحارس الدور في المتصفّح، والتصيير. وخادمٌ يردّ ٤٠٣ صحيحةً وشاشةٌ تعرضها بيضاء
ليسا نجاحاً.

**أربعة قرارات تستحقّ المعرفة:**

١. **قاعدة الاختبار لا قاعدة التطوير.** الخلفية تُشغَّل بـ`dev:e2e` الذي
يُحمّل `.env.test`، فيمرّ بالحارس نفسه الذي يرفض أي قاعدة لا ينتهي اسمها
بـ`_test`.

٢. **البناء لا خادم التطوير.** Vite يُحوّل الوحدات عند الطلب، فأوّل تحميلٍ
لصفحة تجاوز ثلاثين ثانية ولم يُنهِ حدث `load`. والمعاينة تُقدّم أصولاً مبنيّة:
أسرع، وتختبر ما يصل المستخدم فعلاً.

٣. **لا إعادة استعمال لخادمٍ قائم.** جُرِّب البديل: عُطِّل حارس الدور في
العميل فمرّت الاختبارات الستّ والثلاثون خضراء — لأن خادم المعاينة كان يُقدّم
بناءً سابقاً. وبعد إعادة البناء احمرّت ثلاثة كما ينبغي. الثمن دقيقةُ بناءٍ في
كل تشغيل، وهو أرخص من إعدادٍ يُطمئن كذباً.

٤. **`data-testid` لا النصّ المعروض.** الواجهة بثلاث لغات؛ اختبارٌ يبحث عن
«تسجيل الدخول» ينكسر بتحرير ملفّ ترجمة لا علاقة له بالسلوك.

**متطلّب لمرّة واحدة:** `npx playwright install chromium`.

---

## ما لم يُبنَ بعد


